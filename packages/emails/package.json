{
  "name": "@silonya/emails",
  "version": "0.0.0",
  "private": true,
  "description": "Transactional email templates (ORDER_MANAGEMENT.md §9) used by the notification worker",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "@silonya/config": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.0"
  }
}
