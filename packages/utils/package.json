{
  "name": "@silonya/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types, constants, and pure utility functions used across SILONYA apps and packages",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "@silonya/config": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.0"
  }
}
